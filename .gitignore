# iis-honeypot .gitignore

# Local compose file - copy docker-compose-example.yml to docker-compose.yml
# and customise ports/settings for your environment. The local copy is ignored
# so personal configuration is never accidentally committed.
docker-compose.yml
